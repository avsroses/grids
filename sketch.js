const CELSIZES = [10, 20, 50];

function setup() {
  createCanvas(600, 800);
  noLoop();
}

function draw() {
  background(220);

  const CELLSIZE = 20;
  noStroke();
  ///////////////
  //simple grid//
  ///////////////
  /*
  for(let y = 0; y < height; y += CELLSIZE){
    for(let x = 0; x < width; x += CELLSIZE) {
      fill(random(255));
      rect(x, y, CELLSIZE);
    }
  } */

  /////////////////
  //Piet Mondrian//
  /////////////////

}
